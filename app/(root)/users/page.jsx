import React from "react";

const page = () => {
  return (
    <div>
      This is the Page to display all users then it can be filtered based on
      department / level or someother thing
    </div>
  );
};

export default page;
